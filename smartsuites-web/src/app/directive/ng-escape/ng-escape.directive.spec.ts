import { NgEscapeDirective } from './ng-escape.directive';

describe('NgEscapeDirective', () => {
  it('should create an instance', () => {
    const directive = new NgEscapeDirective();
    expect(directive).toBeTruthy();
  });
});
