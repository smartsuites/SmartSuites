import { NgIncludeDirective } from './ng-include.directive';

describe('NgIncludeDirective', () => {
  it('should create an instance', () => {
    const directive = new NgIncludeDirective();
    expect(directive).toBeTruthy();
  });
});
